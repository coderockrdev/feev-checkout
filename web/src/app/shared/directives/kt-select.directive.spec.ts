import { KtSelectDirective } from './kt-select.directive';

describe('KtSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new KtSelectDirective();
    expect(directive).toBeTruthy();
  });
});
