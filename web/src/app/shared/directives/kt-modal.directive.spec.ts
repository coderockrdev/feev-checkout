import { KtModalDirective } from "./kt-modal.directive";

describe("KtModalDirective", () => {
  it("should create an instance", () => {
    const directive = new KtModalDirective();
    expect(directive).toBeTruthy();
  });
});
